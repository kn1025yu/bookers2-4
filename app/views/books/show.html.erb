<%= flash[:notice] %>
<%= flash[:alert] %>

<div class="col-md-3">
  <h2>User info</h2>
  <% if @user .present? %>
    <%= image_tag @user.image.url %>
  <% else %>
    <%= image_tag 'no_image.jpg'%>
  <% end %>
<table class="row">
<tbody>
  <tr></tr>
  <tr>
	  <th>name</th>
	  <th></th>
  </tr>
  <tr>
	  <th>introduction</th>
	  <th></th>
  </tr>
</tbody>
</table>
  <div>
    <a class="btn btn-outline-secondary btn-block fas fa-user-cog edit_user" href="/users/edit"></a>
  </div>

  <h2 class="mt-3">New book</h2>
  <form action="/books" accept-charset="UTF-8" method="post"><input name="utf8" type="hidden" value="✓"><input type="hidden" name="authenticity_token" value="00ARvNy3+Rcuc4cB/v7s+og994opNIdH550xVFqbQ08oeYaOtk7Y+VVEyzHXWYiMrR3yWl881PEe0fNhxIlO/Q==">
  <div class="form-group">
    <label for="book_title">Title</label>
    <input class="form-control book_title" type="text" name="book[title]" id="book_title">
  </div>
  <div class="form-group">
    <label for="book_opinion">Opinion</label>
    <textarea class="form-control book_body" name="book[body]" id="book_body"></textarea>
  </div>
  <div class="form-group">
    <input type="submit" name="commit" value="Create Book" class="btn btn-success" data-disable-with="Create Book">
  </div>
</form>
</div>

<div class="contents">
  <h2>Books</h2>
  <table>
   <thead>
    <tr>
      <th>Title</th>
      <th>Opinion</th>
    </tr>
   </thead>

    <tbody>
      <% @books.each do |book| %>
        <tr>
          <td>
            <%= book.user.name%>
          </td>
          <td><%= link_to book.title, book_path(book.id) %></td>
          <td><%= book.body %></td>
          <td></td>
          <td>
           <%= link_to "edit", book_path(book), class: 'btn btn-success btn-sm btn-block mb-3 destroy' %>
          </td>
          <td></td>
          <td>
            <%= link_to "destroy", book_path(book), method: :delete, class: 'btn btn-danger btn-sm btn-block mb-3 destroy', "data-confirm" => "Are you sure?" %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>